#!/usr/bin/env bash

# get $FPKG_ROOT from here
source /etc/fpkg.conf

# adapt our package list to something Bash understands
PKGLIST="$(awk '{ print $1 }' ${FPKG_ROOT}/pkg.lst | tr '\n' ' ')"

# and finally, apply the completion list
complete -W "${PKGLIST}" fpkg
